import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>NEXT SHOP | 홈</title>
      </Head>
      <div>메인 페이지 입니다.</div>
    </div>
  );
};

export default Home;
